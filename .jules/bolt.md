## 2023-11-20 - Consolidated data aggregation in AdminPage
**Learning:** React render loops can often be optimized by combining `.filter()` and `.length` calls into existing iteration logic.
**Action:** Always scan for redundant array passes and combine them with other data aggregation loops using multiple accumulators.
